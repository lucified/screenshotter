
machine:
  node:
    version: 7.8
  services:
    - docker

test:
  override:
    - npm run lint
    - npm run test

deployment:
  staging:
    branch: /^(?!nodeploy-).*$/
    commands:
      - npm run deploy

