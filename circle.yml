
machine:
  node:
    version: 7.4
  services:
    - docker

test:
  override:
    - npm run test
    - npm run lint

deployment:
  staging:
    branch: /^(?!nodeploy-).*$/
    commands:
      - npm run deploy

